it("triggers login when the user is not logged in", () => {
    expect(1).toBe(2);
});

it("doesn't trigger login on public endpoints", () => {
    expect(1).toBe(2);
});

it("handles requests to '/callback' as expected", () => {
    expect(1).toBe(2);
});

it("handles requests to '/logout' as expected", () => {
    expect(1).toBe(2);
});

it("handles silent auth as expected", () => {
    expect(1).toBe(2);
});
